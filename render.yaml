services:
  - type: web
    name: HostelBackend
    env: python
    runtime: python-3.13
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn Backend.wsgi --log-file -
    envVars:
      - key: DJANGO_DEBUG
        value: "False"
